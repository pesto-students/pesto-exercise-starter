
// Don't change the export values.
// You can use the function in the Math module

const sqrt = (squareRootNumber) => Math.sqrt(squareRootNumber);
const power = (powerNumber1, powerNumber2) => powerNumber1 ** powerNumber2;
const round = (roundNumber1, roundNumber2) => Math.round(roundNumber1, roundNumber2);

export {
  sqrt,
  power,
  round,
};
